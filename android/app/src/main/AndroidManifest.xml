<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Foreground location only — kullanıcı fiyat eklerken bölge/şube
         tahmini için. ACCESS_BACKGROUND_LOCATION istemiyoruz; arka planda
         konum okumuyoruz, böylece Play Store policy review tetiklenmez. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Camera (image_picker proof + ileride barcode tarayıcı) -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- image_picker Android Photo Picker / ACTION_GET_CONTENT ile galeri seçer;
         geniş depolama izinleri istemiyoruz. Kamera akışı için CAMERA yeterli. -->

    <!-- Notification permission -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Play Billing (FiyatRadar Pro abonelik) -->
    <uses-permission android:name="com.android.vending.BILLING"/>

    <!-- allowBackup=false: Auto Backup, Firebase Auth oturum durumunu ve
         SharedPreferences bayraklarını bayat şekilde geri yükleyip
         "kendiliğinden çıkış yapılmış" oturumlara yol açabiliyordu.
         Android 12+ için ayrıntı: res/xml/data_extraction_rules.xml.
         enableOnBackInvokedCallback: Android 14+ predictive back desteği. -->
    <application
        android:label="FiyatRadar"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Firebase Messaging Service -->
        <service
            android:name="io.flutter.plugins.firebase.messaging.FlutterFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- Firebase Notification Channel -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel"/>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher"/>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color"/>

        <!-- AdMob application id (production).
             Test reklamı sandbox'ı bu ID'ye bağlı değil — kDebugMode'da
             AdHelper test ad unit'i serve ediyor; release'de dart-define
             ile gerçek banner unit'i (default fallback ile) yükleniyor.
             https://developers.google.com/admob/android/test-ads -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7857385959432819~9578886399"/>
    </application>
</manifest>
