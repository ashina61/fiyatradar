<?xml version="1.0" encoding="utf-8"?>
<!-- Dark-mode native splash. Logo intentionally omitted — Flutter's
     _SplashScreen renders the FR mark once the engine boots so the user
     never sees the brand twice in succession. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/fr_splash_bg_dark"/>
</layer-list>
